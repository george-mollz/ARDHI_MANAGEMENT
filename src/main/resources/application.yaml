spring:
  application:
    name: Land-registration


  Datasource:
    url: jdbc:postgresql://localhost:5432/ardhidb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true

server:
  port: 8081